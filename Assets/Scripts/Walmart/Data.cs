using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class Data
{
    enum estados{
        bloqueado,
        habilitado,
        completado,
    }
    private static estados[] escenas;
}